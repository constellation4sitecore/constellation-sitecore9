namespace Constellation.Foundation.Mvc.Patterns.Repositories
{
	/// <summary>
	/// An object that queries a Sitecore data storage source (Database, Index) and retrieves results which are then
	/// returned as a ViewModel for consumption by an MVC View.
	/// </summary>
	public interface IRepository
	{
	}
}
